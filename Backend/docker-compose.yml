services:
  postgres:
    image: postgres:18.1
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: prisma_blog
    ports:
      - "5434:5432"
    volumes:
      - postgres:/var/lib/postgresql

volumes:
  postgres: