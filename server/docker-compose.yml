services:
  postgres:
    image: public.ecr.aws/docker/library/postgres:alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: prisma_blog
    ports:
      - "5434:5432"
    volumes:
      - postgres:/var/lib/postgresql

volumes:
  postgres:
